---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: modules.addons.otterscale.io
spec:
  group: addons.otterscale.io
  names:
    kind: Module
    listKind: ModuleList
    plural: modules
    singular: module
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.templateRef
      name: Template
      type: string
    - jsonPath: .spec.namespace
      name: Namespace
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Module is the Schema for the modules API.
          A Module represents an installed platform addon instantiated from a ModuleTemplate.
          The controller creates the corresponding FluxCD HelmRelease or Kustomization
          and reflects its status back to the Module.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired behavior of the Module.
            properties:
              namespace:
                description: |-
                  Namespace overrides the default target namespace defined in the ModuleTemplate.
                  If not specified, the namespace from the ModuleTemplate is used.
                maxLength: 63
                minLength: 1
                pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?)$
                type: string
              templateRef:
                description: |-
                  TemplateRef is the name of the ModuleTemplate to instantiate.
                  This field is immutable after creation.
                maxLength: 253
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: templateRef is immutable
                  rule: self == oldSelf
              values:
                description: |-
                  Values overrides the default values for HelmRelease-based modules.
                  Only applicable when the referenced ModuleTemplate uses a HelmRelease template.
                  Ignored for Kustomization-based modules.
                x-kubernetes-preserve-unknown-fields: true
            required:
            - templateRef
            type: object
          status:
            description: Status represents the current information about the Module.
            properties:
              conditions:
                description: Conditions store the status conditions of the Module
                  (e.g., Ready, TemplateNotFound).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              helmReleaseRef:
                description: HelmReleaseRef is a reference to the FluxCD HelmRelease
                  managed by this Module.
                properties:
                  name:
                    description: Name is the name of the referenced resource.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced resource.
                    type: string
                required:
                - name
                type: object
              kustomizationRef:
                description: KustomizationRef is a reference to the FluxCD Kustomization
                  managed by this Module.
                properties:
                  name:
                    description: Name is the name of the referenced resource.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced resource.
                    type: string
                required:
                - name
                type: object
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed by the controller.
                  It corresponds to the Module's generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
              templateGeneration:
                description: |-
                  TemplateGeneration tracks the observed generation of the referenced ModuleTemplate.
                  This allows the controller to detect and reconcile template changes.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
